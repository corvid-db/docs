import type { AstroMarkdownOptions, MdxRenderer, MdxRendererOptions, RemarkRehype, ShikiConfig, Smartypants, SyntaxHighlightConfig, SyntaxHighlightConfigType } from '@astrojs/internal-helpers/markdown';
import type { PluggableList } from 'unified';
import type { UnifiedResolvedOptions } from '../processor.js';
import { type OptimizeOptions } from './rehype-optimize-static.js';
/** Fully-resolved inputs the unified MDX pipeline needs to build its processor. */
interface UnifiedMdxOptions {
    syntaxHighlight: SyntaxHighlightConfig | SyntaxHighlightConfigType | false | undefined;
    shikiConfig: ShikiConfig;
    gfm: boolean;
    smartypants: boolean | Smartypants;
    remarkPlugins: PluggableList;
    rehypePlugins: PluggableList;
    remarkRehype: RemarkRehype;
    recmaPlugins: PluggableList;
    optimize: boolean | OptimizeOptions;
}
interface MdxProcessorExtraOptions {
    sourcemap: boolean;
}
/**
 * Build the `MdxRenderer` for the `unified` processor. Called via
 * `unified().createMdxRenderer` — `options` are the processor's own remark/rehype
 * plugins, `shared` the cross-cutting markdown options, `mdx` the MDX-only inputs.
 */
export declare function createUnifiedMdxProcessor(shared: AstroMarkdownOptions, mdx: MdxRendererOptions, options: UnifiedResolvedOptions): MdxRenderer;
export declare function createMdxProcessor(mdxOptions: UnifiedMdxOptions, extraOptions: MdxProcessorExtraOptions): import("unified").Processor<import("mdast").Root, import("estree").Program, import("estree").Program, import("estree").Program, string>;
export {};
